<% if @comic.errors.any? %>
  <div><% @comic.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
<% end %>

<div class="row">
  <div class="small-6 large-centered columns">
    <div class="panel2">
      <h2>Comic Form</h2>

      <%= render "edit_comic_form" %>
    </div>
  </div>
</div>
